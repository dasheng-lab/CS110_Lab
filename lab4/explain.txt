1.1 What causes the errors in simple_fn, naive_mod, and mul_arr that were reported by the Venus CC checker?
    Because simple_fn use t0 be not saved, naive_mod and mul_arr don't save s0 s1 in prologue and load epilogue, t0 is also not saved in helper_fn.
1.2 In RISC-V, we invoke functions by jumping to them and storing the return address in the ra register. Does calling convention apply to the jumps to the naive_mod_loop or naive_mod_end labels?
    No. Because they are not call any function.
1.3 Why do we need to store ra in the prologue for mul_arr, but not in any other function?
    They don't call other functions.
1.4 Why wasn't the calling convention error in helper_fn reported by the CC checker? (Hint: it's mentioned above in the exercise instructions.)
    helper_fn not in .global